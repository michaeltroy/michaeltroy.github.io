{
  "name": "tiny-build",
  "version": "0.0.1",
  "description": "Proof of concept. A tiny build.",
  "main": "index.js",
  "devDependencies": {},
  "scripts": {
    "boot": "npm run duo && npm run watch && cd .. && jekyll server",
    "watch": "npm run duo-watch",
    "duo": "duo *.{js,css}",
    "duo-watch": "duo -g app --watch *.{js,css}"
  }
}